import $ from "jquery"
import slick from 'slick-carousel'

 $('.us-social__slider').slick({
   slidesToShow: 1,
   centerMode: true
 });

$('.reviews__slider').slick({
    centerMode: true,
    slidesToShow: 1,
});
